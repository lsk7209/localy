# API 마이그레이션 노트

## 📋 현재 상황

공공데이터포털에서 새로운 Open API 형식을 제공하고 있습니다.

### 현재 사용 중인 API (레거시)

- **Base URL**: `https://apis.data.go.kr/B553077/api/open/sdsc2/`
- **엔드포인트**:
  - `storeListInDong`: 행정동별 상가 목록
  - `storeListByDate`: 수정일별 상가 목록

### 새로운 Open API

- **Base URL**: `api.odcloud.kr/api`
- **Namespace**: `15083033`
- **Swagger 문서**: https://infuser.odcloud.kr/oas/docs?namespace=15083033/v1
- **엔드포인트**: `/15083033/v1/uddi:c7049f5a-d95e-4143-be96-b4d3c16130ee`
- **데이터명**: 소상공인시장진흥공단_상가(상권)정보_20171120

## 🔄 마이그레이션 계획

### 1단계: Swagger 문서 확인

Swagger 문서에서 다음 정보를 확인해야 합니다:

1. **요청 파라미터**
   - 인증키 전달 방식 (`serviceKey` 또는 헤더)
   - 필수 파라미터 목록
   - 선택 파라미터 목록

2. **응답 형식**
   - JSON 구조
   - 페이지네이션 방식
   - 에러 응답 형식

3. **API 제한사항**
   - 일일 호출 제한
   - 요청 크기 제한
   - 응답 크기 제한

### 2단계: 코드 업데이트

새로운 API 형식에 맞춰 다음 파일들을 업데이트해야 합니다:

- `workers/utils/public-data-api.ts`: API URL 및 호출 로직
- `workers/types.ts`: 응답 타입 정의
- `workers/cron/initial-fetch.ts`: 초기 수집 로직
- `workers/cron/incremental-fetch.ts`: 증분 수집 로직

### 3단계: 테스트

1. **로컬 테스트**
   - 새로운 API 엔드포인트로 테스트 호출
   - 응답 형식 확인
   - 에러 처리 확인

2. **스테이징 테스트**
   - 소량 데이터로 수집 테스트
   - 데이터 정규화 확인
   - 성능 확인

3. **프로덕션 배포**
   - 점진적 롤아웃
   - 모니터링 강화
   - 롤백 계획 수립

## 📝 확인 사항

### Swagger 문서에서 확인할 항목

1. **인증 방식**
   - [ ] 인증키를 파라미터로 전달하는가?
   - [ ] 헤더로 전달하는가?
   - [ ] Encoding 키를 사용해야 하는가?

2. **요청 파라미터**
   - [ ] 행정동 코드를 어떻게 전달하는가?
   - [ ] 날짜 필터링은 어떻게 하는가?
   - [ ] 페이지네이션은 어떻게 하는가?

3. **응답 형식**
   - [ ] JSON 구조는 어떻게 되는가?
   - [ ] 현재 코드의 `PublicDataStore` 타입과 호환되는가?
   - [ ] 에러 응답은 어떻게 처리되는가?

4. **기능 호환성**
   - [ ] 행정동별 조회가 가능한가?
   - [ ] 날짜별 조회가 가능한가?
   - [ ] 페이지네이션이 지원되는가?

## 🚨 주의사항

1. **인증키 관리**
   - Encoding 키와 Decoding 키 중 하나를 선택하여 일관되게 사용
   - 환경 변수에 저장 시 특수문자 이스케이프 확인

2. **API 제한**
   - 일일 호출 제한 확인
   - Rate limiting 구현 필요 여부 확인

3. **데이터 호환성**
   - 새로운 API의 응답 형식이 기존 데이터 구조와 호환되는지 확인
   - 필요 시 데이터 변환 로직 추가

## 🔗 참고 링크

- **Swagger 문서**: https://infuser.odcloud.kr/oas/docs?namespace=15083033/v1
- **공공데이터포털**: https://www.data.go.kr
- **API 문서**: `PUBLIC_DATA_API_LINKS.md`

